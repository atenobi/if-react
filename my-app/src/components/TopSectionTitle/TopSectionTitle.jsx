import React from 'react';

// styles
import './index.css';

const TopSectionTitle = () => (
  <div className="top_section__title">
    <h1 className="top_section__title--text">
      Discover stays
    </h1>
    <h1 className="top_section__title--text">
      to live, work or just relax
    </h1>
  </div>
);

export default TopSectionTitle;
